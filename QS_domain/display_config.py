"""
显示模式配置 — 单一事实来源。

UI 下拉框选项和绘图服务默认缩放均从此模块读取，避免两处维护失配。
"""

# 参数类型 → 可选的数据切面列表（用于 UI 下拉框）
FACET_OPTIONS: dict[str, list[str]] = {
    'S参数': ['幅度(dB)', '幅度(abs)', '相位(度)', '相位(rad)',
              'unwrap相位(度)', 'unwrap相位(rad)', '实部', '虚部', '群延迟(fs)'],
    'Y参数': ['导纳(abs)', '幅度(dB)', '相位(度)', '实部', '虚部'],
    'Z参数': ['阻抗(mΩ)', '幅度(dB)', '相位(度)', '实部(ESR)', '虚部', '电容(pF)'],
    '时域':  ['TDR阻抗', '阶跃响应', '冲激响应', '脉冲响应'],
}

# (参数类型, 数据切面) → (X轴缩放默认值, Y轴缩放默认值)
# 未列出的组合默认为 ('线性', '线性')
DEFAULT_SCALES: dict[tuple[str, str], tuple[str, str]] = {
    ('Z参数', '阻抗(mΩ)'): ('对数', '对数'),
    ('Y参数', '导纳(abs)'): ('对数', '对数'),
    ('Z参数', '实部(ESR)'): ('对数', '线性'),
}
